import { heart, searchCircle, search, close, chevronBackOutline } from "ionicons/icons";

export const ionicIcons: { [name: string]: string } = {
    heart,
    searchCircle,
    search,
    close,
    chevronBackOutline,
};
